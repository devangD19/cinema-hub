# TODO - Cinema Hub (movie booking UI)

- [ ] Update `index.html` to include a Book Tickets section with (movie → showtime → seats → summary → confirm):

  - movie selection (static)

  - showtime selection (static radio/buttons)
  - seat grid (static selectable-looking UI without real interactivity)
  - booking summary (static price breakdown)
  - confirm booking CTA
- [x] Update `style.css` to style the new booking section:
  - booking panel layout
  - seat grid visuals (available/selected/blocked states via CSS classes)
  - summary and CTA buttons
  - responsive behavior
- [ ] Quick manual test: open `index.html` in browser and verify layout/visuals


